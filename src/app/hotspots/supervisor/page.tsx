
"use client"

import React from 'react'
import SocialNetworkMapPage from '../page'

/**
 * Supervisor version of the hotspots page.
 * Wraps the existing map but allows for supervisor-specific context if needed.
 */
export default function SupervisorHotspotsPage() {
  return <SocialNetworkMapPage />
}
