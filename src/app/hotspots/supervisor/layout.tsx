
export default function SupervisorHotspotsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Navigation is already provided by the parent /hotspots/layout.tsx
  return <>{children}</>
}
